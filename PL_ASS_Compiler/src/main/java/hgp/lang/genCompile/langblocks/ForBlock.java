package hgp.lang.genCompile.langblocks;

import org.antlr.v4.runtime.Token;

public class ForBlock extends BlockClassBase {
    public ForBlock(Token token, boolean openBlock) {
        super(token, openBlock);
    }
}
