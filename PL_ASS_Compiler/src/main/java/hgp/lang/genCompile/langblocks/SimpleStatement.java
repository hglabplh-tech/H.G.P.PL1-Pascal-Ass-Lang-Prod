package hgp.lang.genCompile.langblocks;

import org.antlr.v4.runtime.Token;

public class SimpleStatement extends Statement{
    public SimpleStatement(Token token, String name) {
        super(token, name);
    }
}
